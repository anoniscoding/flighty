<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent" xmlns:app="http://schemas.android.com/apk/res-auto"
        android:background="@drawable/splash_bg_selector"
        tools:context="com.example.airlineapp.ui.splash.SplashActivity">

    <ImageView
            android:id="@+id/image"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_arrivals"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintVertical_bias="0.415"/>

    <TextView
            android:id="@+id/appName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/flightly"
            android:textColor="@color/colorAccent"
            android:layout_marginTop="@dimen/dp10"
            android:textStyle="bold"
            android:textSize="@dimen/sp36"
            app:layout_constraintTop_toBottomOf="@+id/image"
            app:layout_constraintLeft_toLeftOf="@+id/image"
            app:layout_constraintRight_toRightOf="@+id/image"

    />

    <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/flightly_description"
            android:textColor="@color/colorAccent"
            android:layout_marginTop="@dimen/dp10"
            android:textSize="@dimen/sp18"
            app:layout_constraintTop_toBottomOf="@+id/appName"
            app:layout_constraintLeft_toLeftOf="@+id/appName"
            app:layout_constraintRight_toRightOf="@+id/appName"
    />

</androidx.constraintlayout.widget.ConstraintLayout>
